# Public URLs for app store buttons on the landing page (optional; placeholders shown until set).
NEXT_PUBLIC_APP_STORE_URL=
NEXT_PUBLIC_PLAY_STORE_URL=

# Gemini API key for POST /api/assistant/chat (fridge assistant). Optional; route returns 503 if unset.
GEMINI_API_KEY=
